export interface TBookingFormData {
  name: string;
  phone: string;
  email?: string;
  service?: string;
  date?: string;
  message?: string;
}

export interface TContactFormData {
  name: string;
  contact: string;
  message: string;
}
